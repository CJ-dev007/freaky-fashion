
<div class="desktop-spots">
  <% spots.forEach(spot => { %>
    <a href="<%= spot.url %>" class="spot">
       <img src="<%= spot.image %>" alt="<%= spot.name %>">
       <div class="spot-text"><%= spot.name %></div>
    </a>
  <% }) %>
</div>

